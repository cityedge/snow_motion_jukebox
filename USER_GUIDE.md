# Snow / Motion v1.2.0 ユーザーガイド

Snow / Motion — White Line, Warm Heartは、音楽とともに生成される雪山を滑るブラウザ向け3Dスノーボード・ジュークボックスゲームです。

This is a browser-based 3D snowboard jukebox game with eight bundled songs, generated courses, dynamic weather, local music playback, and optional SRT subtitles.

## 1. 推奨環境

- デスクトップ版の最新Google ChromeまたはMicrosoft Edge
- WebGL 2とWeb Audio APIが利用できるPC
- キーボード操作を推奨（タッチ操作にも対応）
- GitHub PagesなどのHTTPSサイト、またはローカル静的HTTPサーバー

`index.html`の直接起動（`file://`）では、ES Modules、音源、テクスチャの読み込みがブラウザに拒否される場合があります。

## 2. 最初に滑るまで

1. 公開URLを開きます。
2. 大きな **最初から滑る** を押すと、1曲目の通常モードが開始します。
3. 各曲を選ぶと約10秒間の試聴が流れます。
4. **この曲で滑る** または **ランダムステージで滑る** を選びます。
5. 必要な場合はメインメニュー左下で音量を調整します。

メニュー、ポーズ画面、クリア画面はマウス／タップのほか、カーソルキーとEnterでも操作できます。

## 3. ゲームモード

### 通常モード

曲ごとの時間帯、天候、霧、風、照明、先行ボーダーなどの演出を維持しつつ、コース形状や障害物配置を毎回生成します。同じ曲らしさを保ちながら、異なるコースを滑れます。

### ランダムステージ

選択した曲を使い、コース特性、環境、雪、霧、風、照明、先行ボーダーなどを、成立する組み合わせの範囲でランダム生成します。

### 外部の曲をロード

手元の音源を端末内から選び、ランダムステージで再生します。外部ファイルはサーバーへ送信されません。

対応候補: AAC、AIFF、FLAC、M4A、MP3、OGA、OGG、OPUS、WAV、WebM。実際の再生可否はブラウザとOSのコーデック対応に依存します。

音源と同じベース名のSRTファイルを一緒に選ぶと字幕を表示します。

```text
my-song.mp3
my-song.srt
```

非常に長い音源は、開始から10分でフェードアウトしてクリア扱いになります。

## 4. 滑走操作

| キー | 操作 |
| --- | --- |
| `A` / `←` | 左へカーブ |
| `D` / `→` | 右へカーブ |
| `S` / `↓` | ブレーキ |
| `P` / `Esc` | ポーズ／再開 |
| `R` | 現在の曲を最初から滑る |
| `N` | 同じモード・曲設定で新しいコースを生成 |
| `V` | スペクトラム表示切り替え |
| `F` | FPS表示切り替え |
| `Shift` + `D` | ステージ設定のデバッグ表示 |
| `L` | 日本語／英語切り替え |

通常モードの`N`は、曲固有の演出を維持した別コースを生成します。ランダムステージでは、ランダム設定を含む新しいコースへ切り替わります。

### タッチ操作

ゲーム画面の上部20%をタップすると、`Esc`と同様にポーズ／再開します。残りの領域では、左半分を押している間は左カーブ、右半分を押している間は右カーブです。メニュー・ポーズ画面・クリア画面のボタンは直接タップできます。

### シナリオ指定ランダム（隠し操作）

通常曲の滑走画面で数字キーを押すと、曲はそのまま、指定した演出シナリオだけを固定したランダムステージへ移ります。コース形状、具体的な天候値、FOG、先行者、滑走跡などは新しく抽選されます。画面上の操作説明には表示されないテスト・再試行用コマンドです。

| キー | 固定するシナリオ |
| --- | --- |
| `0` | 晴れた早朝 |
| `1` | 澄んだ朝 |
| `2` | 明るい昼の雪 |
| `3` | 曇り始める午後 |
| `4` | ロッジ前の強い雪 |
| `5` | 晴れた夕暮れ |
| `6` | 夕暮れの荒天 |
| `7` | 雪のナイター |
| `8` | 荒天のナイター |
| `9` | 静かな夜の山 |

シナリオ指定後に`N`を押すと、同じシナリオを維持した別シードへ移ります。外部音源はページ再読み込み時にブラウザ内の音源データを保持できないため、この操作には対応しません。

## 5. ポーズと滑走終了

ポーズ画面では滑走へ戻る、最初から滑る、メニューへ戻る操作ができます。

曲を滑り切るとクリア画面になります。**次の曲へ** が初期選択され、最終曲では **最初の曲へ** になります。滑り直しとメニュー復帰も選択できます。

## 6. 音量と表示設定

メインメニューの音量は、曲の試聴、本編の音楽、風・滑走・着地などの効果音へ共通して適用されます。設定はブラウザの`localStorage`へ保存されます。

FPS表示は初期状態ではOFFです。`F`を押すと、画面左上に実表示FPS、CPUフレーム時間、CPU時間から換算した最大FPS相当値を表示します。

## 7. 字幕

内蔵曲にはSRT字幕が付属します。外部曲では、音源と同名のSRTを音源と一緒に選択した場合だけ読み込みます。字幕の文字コードはUTF-8を推奨します。

## 8. デバッグ表示

`Shift + D`で、シード、ステージ特性、天候、生成物数、現在の霧・雪・風などを確認できます。URLに表示されるシードを記録すると、問題の起きたコースを再現しやすくなります。

## 9. プライバシー

外部音源とSRTはブラウザ内のオブジェクトURLとして処理されます。本アプリには、それらを外部サーバーへアップロードする処理はありません。保存されるのは音量などの軽量な設定値です。

## 10. トラブルシューティング

### 画面が開かない

`index.html`を直接開かず、GitHub Pagesまたは静的HTTPサーバーから開いてください。開発環境では`npm run dev`、ビルド確認では`npm run preview`を使用できます。

### 音楽が始まらない

ブラウザの自動再生制限が原因の場合があります。画面をクリックするかキーを押して再試行してください。タブやサイトがミュートされていないかも確認します。

### 外部音源を再生できない

同じ拡張子でも内部コーデックによって再生可否が異なります。ChromeまたはEdgeで試すか、MP3、AAC、OGG、WAVなど一般的な形式へ変換してください。

### 描画が重い

他のGPU負荷が高いタブやアプリを閉じてください。FPS表示をONにすると、描画負荷の目安を確認できます。

## 11. GitHub Pages

リポジトリの`docs/`をGitHub Pagesの公開元に指定します。詳しい手順は[README.md](README.md)と[RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md)を参照してください。
